kind: pipeline
type: docker
name: build
steps:
 - name: react
   image: node:12
   commands:
    - cd reactClient/my-app
    - npm install
    - npm link --unsafe-perm ../../OpenAPIReactClient
    - CI="" npm run build
 - name: symfony
   image: composer:2
   commands:
    - cd ./Symfony-API
    - composer install --no-suggest --no-dev --no-progress --optimize-autoloader --classmap-authoritative
    - (cd vendor/openapi && rm server-bundle && cp -r ../../../OpenAPIServerBundle/ server-bundle)
    - cp -r ../reactClient/my-app/build/. public/
    - rm ./templates/base.html.twig && ln -s ../public/index.html ./templates/index.html
    - cp ../dockerBuild/SymfonyDockerfile Dockerfile
 - name: deploy to test
   image: alpine:3.12
   environment:
     SSH_PRIVATE_KEY:
       from_secret: ssh_private_key
     SSH_HOST:
       from_secret: ssh_host
     SSH_USER:
       from_secret: ssh_user
   commands:
     - apk update
     - apk add zip openssh-clien
    # create zip archive
     - zip --symlinks -r mpm.zip ./Symfony-API
    # transfer to host
     - scp -i mpm.zip "${SSH_USER}@${SSH_HOST}:tmp/"
    # extract to destination
    # execute db update
   #when:
     #event:
     # - promote
     #target:
     # - test     
